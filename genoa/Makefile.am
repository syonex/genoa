## this gets processed by autoreconf to create Makefile.in


# SUFFIXES = .sh

sbin_SCRIPTS=	genoa

# wonder why these aren't in the .tar.gz automatically
# defined in ../Makefile.am
# EXTRA_DIST=	genoa genoa.8

man_MANS=	genoa.8

# .sh :
# 	cat $< > $@
# 	chmod +x $@
